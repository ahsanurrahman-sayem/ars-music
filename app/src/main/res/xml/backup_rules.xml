<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Back up DataStore preferences but not the Room DB (user imports on new device) -->
    <include domain="sharedpref" path="." />
    <exclude domain="database" path="." />
    <exclude domain="cache" path="." />
</full-backup-content>
